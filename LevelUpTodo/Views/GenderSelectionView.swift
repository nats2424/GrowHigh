import SwiftUI

// MARK: - Empty View (No longer needed for UI Highlight Tutorial)
struct GenderSelectionView: View {
    var body: some View {
        EmptyView()
    }
}

#Preview {
    GenderSelectionView()
}